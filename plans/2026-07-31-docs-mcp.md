# Speakeasy Docs MCP Server Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Serve this repository's documentation through a local-first MCP full-text search server (Speakeasy Docs MCP), with Docker and Gram tunnel packaging, modeled on `speakeasy-sdks/google-gemini-docs-mcp` minus terraform.

**Architecture:** A zero-dependency Node script copies the five content hubs into a gitignored `dist/corpus/` directory, renaming `.mdx` → `.md` (no JSX stripping — accepted noise). The `@speakeasy-api/docs-mcp-cli` validates and indexes that corpus FTS-only; `@speakeasy-api/docs-mcp-server` serves it over stdio or Streamable HTTP. A multi-stage Dockerfile bakes the index into a self-contained image; compose pairs it with the Gram tunnel agent.

**Tech Stack:** Node >= 22, `@speakeasy-api/docs-mcp-cli@0.17.0`, `@speakeasy-api/docs-mcp-server@0.17.0`, Docker, `ghcr.io/speakeasy-api/gram-tunnel-agent`.

## Global Constraints

- Spec: `specs/2026-07-31-docs-mcp-design.md`. Read it before starting.
- Work on branch `docs-mcp-setup`. PR to `main` at the end.
- Node engine floor: `>=22`. Package versions pinned exactly: `@speakeasy-api/docs-mcp-cli@0.17.0`, `@speakeasy-api/docs-mcp-server@0.17.0`. npm override: `"h3": "2.0.1-rc.18"`.
- Server name `speakeasy-docs`, tool prefix `speakeasy` (tools: `speakeasy_search_docs`, `speakeasy_get_doc`). HTTP port `20310`, stateless. Embedding provider: `none`.
- Content hubs (exactly five): `docs`, `guides`, `api-design`, `mcp`, `openapi`.
- Do not edit anything inside the content hub directories.
- `package-lock.json` is committed; `node_modules/`, `dist/`, `.env` are gitignored.
- This repository has no JS test framework; verification is by running the listed commands and matching expected output. Do not add a test framework.
- Repo docs style (CLAUDE.md) applies to the README section: sentence case headings, `bash` language on shell blocks, no "you/we" pronouns in docs prose.

---

### Task 1: Package foundation and corpus manifest

**Files:**
- Create: `package.json`
- Create: `.docs-mcp.json`
- Modify: `.gitignore` (currently contains only `.DS_Store`)

**Interfaces:**
- Produces: npm scripts `corpus:build`, `docs:validate`, `docs:build`, `mcp:start`, `mcp:start:http`, `test` (used by every later task); `.docs-mcp.json` manifest consumed by `scripts/build-corpus.mjs` (Task 2) and the Dockerfile (Task 4).

- [ ] **Step 1: Write `package.json`**

```json
{
  "name": "speakeasy-developer-docs-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-first MCP search server for Speakeasy documentation",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "corpus:build": "node scripts/build-corpus.mjs",
    "docs:validate": "npm run corpus:build && docs-mcp validate --docs-dir ./dist/corpus",
    "docs:build": "npm run corpus:build && docs-mcp build --docs-dir ./dist/corpus --out ./dist/index --description \"Speakeasy product, SDK, OpenAPI, and MCP documentation\" --embedding-provider none --tool-description-search \"Search Speakeasy documentation: product docs (SDK generation, Gram, Terraform providers), step-by-step guides, API design best practices, the MCP hub, and the OpenAPI hub. Filter by source to narrow to one hub. Use exact identifiers, CLI commands, and configuration keys.\"",
    "mcp:start": "docs-mcp-server --index-dir ./dist/index --name speakeasy-docs --tool-prefix speakeasy",
    "mcp:start:http": "docs-mcp-server --index-dir ./dist/index --name speakeasy-docs --tool-prefix speakeasy --transport http --port 20310 --stateless",
    "test": "npm run docs:validate && npm run docs:build"
  },
  "dependencies": {
    "@speakeasy-api/docs-mcp-cli": "0.17.0",
    "@speakeasy-api/docs-mcp-server": "0.17.0"
  },
  "overrides": {
    "h3": "2.0.1-rc.18"
  }
}
```

- [ ] **Step 2: Write `.docs-mcp.json`**

```json
{
  "$schema": "https://raw.githubusercontent.com/speakeasy-api/docs-mcp/main/schemas/docs-mcp.schema.json",
  "version": "1",
  "strategy": {
    "chunk_by": "h2",
    "max_chunk_size": 8000,
    "min_chunk_size": 200
  },
  "taxonomy": {
    "source": {
      "vector_collapse": false
    }
  },
  "mcpServerInstructions": "This server provides Speakeasy documentation: core product docs (SDK generation, Gram, Terraform providers), step-by-step guides, API design best practices, the MCP hub, and the OpenAPI hub. Use speakeasy_search_docs before answering Speakeasy implementation questions, and use speakeasy_get_doc when surrounding context is needed. Filter by source to narrow results to one hub.",
  "overrides": [
    {
      "pattern": "docs/**/*.md",
      "metadata": {
        "source": "docs"
      }
    },
    {
      "pattern": "guides/**/*.md",
      "metadata": {
        "source": "guides"
      }
    },
    {
      "pattern": "api-design/**/*.md",
      "metadata": {
        "source": "api-design"
      }
    },
    {
      "pattern": "mcp/**/*.md",
      "metadata": {
        "source": "mcp"
      }
    },
    {
      "pattern": "openapi/**/*.md",
      "metadata": {
        "source": "openapi"
      }
    }
  ]
}
```

- [ ] **Step 3: Replace `.gitignore` content**

```gitignore
.DS_Store
node_modules/
dist/
.env
```

- [ ] **Step 4: Install dependencies and verify the CLI resolves**

Run: `npm install && ./node_modules/.bin/docs-mcp --help`
Expected: install succeeds and creates `package-lock.json`; `docs-mcp --help` prints usage including the `validate` and `build` commands.

Run: `git status --porcelain | grep -c node_modules`
Expected: `0` (node_modules ignored).

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json .docs-mcp.json .gitignore
git commit -m "feat: add docs-mcp package foundation and corpus manifest"
```

---

### Task 2: Corpus builder script

**Files:**
- Create: `scripts/build-corpus.mjs`

**Interfaces:**
- Consumes: `.docs-mcp.json` at repo root (Task 1); the five hub directories.
- Produces: `dist/corpus/` tree containing only `.md` files plus `dist/corpus/.docs-mcp.json`. Invoked via `npm run corpus:build`. Exit code 1 with a message on a missing hub or a filename collision.

- [ ] **Step 1: Write `scripts/build-corpus.mjs`**

```js
#!/usr/bin/env node
// Builds the docs-mcp corpus: copies the content hubs into dist/corpus,
// renaming .mdx to .md so the indexer's **/*.md glob picks them up.
// JSX and import statements are intentionally left in place.
import { copyFileSync, existsSync, mkdirSync, readdirSync, rmSync } from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const repoRoot = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const corpusDir = path.join(repoRoot, "dist", "corpus");
const HUBS = ["docs", "guides", "api-design", "mcp", "openapi"];

rmSync(corpusDir, { recursive: true, force: true });
mkdirSync(corpusDir, { recursive: true });

let copied = 0;

function copyTree(srcDir, destDir) {
  for (const entry of readdirSync(srcDir, { withFileTypes: true })) {
    const src = path.join(srcDir, entry.name);
    if (entry.isDirectory()) {
      copyTree(src, path.join(destDir, entry.name));
      continue;
    }
    if (!entry.isFile()) continue;
    const ext = path.extname(entry.name);
    if (ext !== ".md" && ext !== ".mdx") continue;
    const destName = ext === ".mdx" ? `${entry.name.slice(0, -4)}.md` : entry.name;
    const dest = path.join(destDir, destName);
    if (existsSync(dest)) {
      console.error(`Corpus filename collision: ${dest}`);
      process.exit(1);
    }
    mkdirSync(destDir, { recursive: true });
    copyFileSync(src, dest);
    copied += 1;
  }
}

for (const hub of HUBS) {
  const hubDir = path.join(repoRoot, hub);
  if (!existsSync(hubDir)) {
    console.error(`Missing content hub: ${hub}`);
    process.exit(1);
  }
  copyTree(hubDir, path.join(corpusDir, hub));
}

copyFileSync(path.join(repoRoot, ".docs-mcp.json"), path.join(corpusDir, ".docs-mcp.json"));
console.log(`Corpus built: ${copied} markdown files in ${path.relative(repoRoot, corpusDir)}`);
```

- [ ] **Step 2: Run it and verify the corpus shape**

Run: `npm run corpus:build`
Expected: prints `Corpus built: <N> markdown files in dist/corpus` where N is roughly 520 (the repo has ~521 `.md`/`.mdx` files across the five hubs; exact count may drift as content syncs).

Run: `find dist/corpus -name '*.mdx' | wc -l`
Expected: `0`

Run: `ls dist/corpus`
Expected: `api-design  docs  guides  mcp  openapi` (plus hidden `.docs-mcp.json`).

Run: `test -f dist/corpus/.docs-mcp.json && echo ok`
Expected: `ok`

Run: `find dist/corpus -type f ! -name '*.md' ! -name '.docs-mcp.json' | wc -l`
Expected: `0` (no images, `.txt`, or `.tsx` leaked in).

- [ ] **Step 3: Verify the missing-hub failure path**

Run: `node -e "const m=require('node:fs'); m.renameSync('openapi','openapi.bak')" && npm run corpus:build; echo "exit=$?"; node -e "const m=require('node:fs'); m.renameSync('openapi.bak','openapi')"`
Expected: `Missing content hub: openapi` and `exit=1`, then the hub is restored. Confirm with `ls openapi | head -1`.

- [ ] **Step 4: Commit**

```bash
git add scripts/build-corpus.mjs
git commit -m "feat: add corpus builder for docs-mcp index"
```

---

### Task 3: Validate and build the search index

**Files:**
- No new files; exercises `docs:validate` and `docs:build` from Task 1. Fix findings in `.docs-mcp.json` only if validation fails.

**Interfaces:**
- Consumes: `dist/corpus/` (Task 2), npm scripts (Task 1).
- Produces: `dist/index/` LanceDB index consumed by `mcp:start`/`mcp:start:http` and the smoke test (Task 6).

- [ ] **Step 1: Run validation**

Run: `npm run docs:validate`
Expected: exits 0. If it reports manifest errors, fix `.docs-mcp.json` (not corpus files — they are generated) and re-run. If it reports content errors from individual files, record them in the task notes; do not hand-edit generated corpus files.

- [ ] **Step 2: Build the index**

Run: `npm run docs:build`
Expected: exits 0 and creates `dist/index/` with LanceDB data. FTS-only build of a ~5-10MB corpus should complete in well under a minute.

Run: `du -sh dist/index`
Expected: nonzero size, order of tens to hundreds of MB.

- [ ] **Step 3: Commit (only if `.docs-mcp.json` changed)**

```bash
git add .docs-mcp.json
git commit -m "fix: adjust docs-mcp manifest from validation findings"
```

If nothing changed, skip the commit.

---

### Task 4: Docker image

**Files:**
- Create: `Dockerfile`
- Create: `.dockerignore`

**Interfaces:**
- Consumes: `package.json`/`package-lock.json` (Task 1), `scripts/build-corpus.mjs` (Task 2), the five hub directories, `.docs-mcp.json`.
- Produces: image `speakeasy-developer-docs-mcp` serving Streamable HTTP on port 20310 with `/healthz`; consumed by `compose.yaml` (Task 5).

- [ ] **Step 1: Write `Dockerfile`**

```dockerfile
FROM node:22-slim AS dependencies

WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev

FROM dependencies AS build

COPY .docs-mcp.json ./
COPY scripts ./scripts
COPY docs ./docs
COPY guides ./guides
COPY api-design ./api-design
COPY mcp ./mcp
COPY openapi ./openapi
RUN node scripts/build-corpus.mjs && \
    ./node_modules/.bin/docs-mcp validate --docs-dir ./dist/corpus && \
    ./node_modules/.bin/docs-mcp build \
      --docs-dir ./dist/corpus \
      --out /index \
      --description "Speakeasy product, SDK, OpenAPI, and MCP documentation" \
      --tool-description-search "Search Speakeasy documentation: product docs (SDK generation, Gram, Terraform providers), step-by-step guides, API design best practices, the MCP hub, and the OpenAPI hub. Filter by source to narrow to one hub. Use exact identifiers, CLI commands, and configuration keys." \
      --embedding-provider none

FROM node:22-slim

WORKDIR /app
COPY --from=dependencies /app/node_modules ./node_modules
COPY package.json ./
COPY --from=build /index /index

EXPOSE 20310

HEALTHCHECK --interval=10s --timeout=3s --start-period=5s --retries=3 \
  CMD node -e "fetch('http://127.0.0.1:20310/healthz').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"

ENTRYPOINT ["./node_modules/.bin/docs-mcp-server"]
CMD ["--index-dir", "/index", "--name", "speakeasy-docs", "--tool-prefix", "speakeasy", "--transport", "http", "--port", "20310", "--stateless"]
```

- [ ] **Step 2: Write `.dockerignore`**

```
.git
node_modules
dist
.env
public
specs
plans
```

- [ ] **Step 3: Build and smoke-run the image**

Run: `docker build -t speakeasy-developer-docs-mcp .`
Expected: build succeeds; the build stage logs corpus + validate + index output.

Run: `docker run -d --rm --name docs-mcp-smoke -p 20310:20310 speakeasy-developer-docs-mcp && sleep 5 && curl -sf http://localhost:20310/healthz && docker stop docs-mcp-smoke`
Expected: healthz returns HTTP 200 (curl exits 0, `-f` would fail otherwise), container stops cleanly.

- [ ] **Step 4: Commit**

```bash
git add Dockerfile .dockerignore
git commit -m "feat: add self-contained docs-mcp Docker image"
```

---

### Task 5: Compose with Gram tunnel agent

**Files:**
- Create: `compose.yaml`
- Create: `.env.example`

**Interfaces:**
- Consumes: Docker image build context (Task 4).
- Produces: `docker compose up` stack exposing the MCP server through the Speakeasy Gram tunnel; nothing downstream consumes this.

- [ ] **Step 1: Write `compose.yaml`**

```yaml
name: speakeasy-docs-mcp

services:
  docs-mcp:
    build:
      context: .
    image: speakeasy-developer-docs-mcp:local
    container_name: speakeasy-docs-mcp
    restart: unless-stopped
    networks:
      - gram-tunnel

  gram-tunnel:
    image: ghcr.io/speakeasy-api/gram-tunnel-agent:latest
    container_name: gram-tunnel-speakeasy-docs
    restart: unless-stopped
    depends_on:
      docs-mcp:
        condition: service_healthy
    environment:
      TUNNEL_KEY: ${TUNNEL_KEY:?Set TUNNEL_KEY in .env}
      TUNNEL_LOCAL_MCP_URL: http://docs-mcp:20310/mcp
      TUNNEL_GATEWAY_URL: wss://tunnel.speakeasy.com/connect
      TUNNEL_SERVICE_VERSION: 2026.06.1
    networks:
      - gram-tunnel

networks:
  gram-tunnel:
    name: gram-tunnel-speakeasy-docs
```

- [ ] **Step 2: Write `.env.example`**

```
TUNNEL_KEY=gram_tunnel_replace_me
```

- [ ] **Step 3: Validate compose config**

Run: `TUNNEL_KEY=dummy docker compose config --quiet && echo ok`
Expected: `ok` (no schema errors). Do not `docker compose up` — a real tunnel key is required and out of scope here.

- [ ] **Step 4: Commit**

```bash
git add compose.yaml .env.example
git commit -m "feat: add compose stack with Gram tunnel agent"
```

---

### Task 6: README, end-to-end smoke test, and PR

**Files:**
- Modify: `README.md` (append a new section at the end)

**Interfaces:**
- Consumes: everything above.
- Produces: documented workflow; open PR from `docs-mcp-setup` to `main`.

- [ ] **Step 1: Append a "Docs MCP server" section to `README.md`**

````markdown
## Docs MCP server

This repository doubles as the corpus for a local [Speakeasy Docs MCP](https://github.com/speakeasy-api/docs-mcp) search server running in full-text-search mode, so it requires no API keys. The build copies the five content hubs into `dist/corpus/`, renaming `.mdx` to `.md`, then indexes them.

Build the search index:

```bash
npm install
npm run docs:build
```

Run it over MCP stdio:

```bash
npm run mcp:start
```

Or run the Streamable HTTP transport on port 20310:

```bash
npm run mcp:start:http
```

The HTTP MCP endpoint is `http://localhost:20310/mcp`, and the health check is `http://localhost:20310/healthz`.

The server identifies itself as `speakeasy-docs` and exposes `speakeasy_search_docs` and `speakeasy_get_doc`. Search results can be filtered by `source` (`docs`, `guides`, `api-design`, `mcp`, or `openapi`).

For an MCP client that launches local stdio servers, use this repository as the working directory and configure:

```json
{
  "mcpServers": {
    "speakeasy-docs": {
      "command": "npm",
      "args": ["run", "--silent", "mcp:start"]
    }
  }
}
```

The generated index lives under `dist/index/` and is ignored by Git. Re-run `npm run docs:build` after content changes.

### Docker

Build and run the self-contained HTTP server:

```bash
docker build -t speakeasy-developer-docs-mcp .
docker run --rm -p 20310:20310 speakeasy-developer-docs-mcp
```

To expose the server through the Speakeasy Gram tunnel, copy the environment template, set the issued tunnel key, and start both containers:

```bash
cp .env.example .env
# Edit .env and replace gram_tunnel_replace_me with the issued key.
docker compose up --build -d
docker compose logs -f gram-tunnel
```

The Compose network routes the tunnel agent to the MCP endpoint at `http://docs-mcp:20310/mcp`; the docs server is not published on a host port. Stop and remove both containers with `docker compose down`.
````

- [ ] **Step 2: End-to-end smoke test over HTTP**

Start the server in the background: `npm run mcp:start:http &` (note the PID), wait 3 seconds.

Run: `curl -sf http://localhost:20310/healthz && echo healthy`
Expected: `healthy`

Run:

```bash
curl -s -X POST http://localhost:20310/mcp \
  -H 'content-type: application/json' \
  -H 'accept: application/json, text/event-stream' \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"speakeasy_search_docs","arguments":{"query":"generate a typescript sdk"}}}'
```

Expected: a JSON-RPC result containing search hits referencing corpus paths (e.g. `docs/sdks/...`). If the stateless server rejects a bare `tools/call` (session/initialize error), send an `initialize` request first with the same curl shape (`"method":"initialize","params":{"protocolVersion":"2025-03-26","capabilities":{},"clientInfo":{"name":"smoke","version":"0"}}`) and reuse any returned `mcp-session-id` header via `-H 'mcp-session-id: <value>'`.

Then kill the background server.

- [ ] **Step 3: Full test script passes**

Run: `npm test`
Expected: exits 0 (validate + build).

- [ ] **Step 4: Commit and raise PR**

```bash
git add README.md plans/2026-07-31-docs-mcp.md
git commit -m "docs: document the docs MCP server workflow"
git push -u origin docs-mcp-setup
gh pr create --title "Add Speakeasy docs MCP server" --body "$(cat <<'EOF'
## Summary
- Docs MCP search server over this repository's five content hubs (docs, guides, api-design, mcp, openapi), modeled on speakeasy-sdks/google-gemini-docs-mcp without the terraform deployment
- Build-time corpus copy renames .mdx to .md (no JSX stripping) into gitignored dist/corpus; FTS-only index (no API keys)
- npm scripts for stdio and Streamable HTTP (port 20310), self-contained Docker image, compose stack with the Gram tunnel agent
- Design spec: specs/2026-07-31-docs-mcp-design.md

## Test plan
- [x] npm test (validate + index build)
- [x] curl /healthz + speakeasy_search_docs smoke over HTTP
- [x] docker build + containerized healthz
- [x] docker compose config validation
EOF
)"
```

Expected: PR URL printed.
