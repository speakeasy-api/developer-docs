import { Table } from "@/mdx/components";

# Terraform configuration options

 This section details the available configuration options for the Terraform SDK. All configuration is managed in the `gen.yaml` file under the `terraform` section.

## Version and general configuration

```yml
terraform:
  version: 1.2.3
  author: "Author Name"
  packageName: "custom-sdk"
```

<Table 
  data={[
    { name: "version", required: "true", default: "0.0.1", description: "The current version of the SDK." },
    { name: "packageName", required: "true", default: "terraform", description: "The Terraform provider name." },
    { name: "author", required: "true", default: "speakeasy", description: "The name of the author of the published package." }
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" }
  ]}
/>

## Additions

```yml
terraform:
  additionalDependencies: {}
  additionalResources: []
  additionalDataSources: []
  additionalEphemeralResources: []
```

<Table 
  data={[
    { name: "additionalDependencies", required: "false", default: "{}", description: "Add additional dependencies to include in the generated `go.mod`." },
    { name: "additionalResources", required: "false", default: "[]", description: "A list of `{ importLocation?: string, importAlias?: string, resource: string }` objects to insert into the provider resource list." },
    { name: "additionalDataSources", required: "false", default: "[]", description: "A list of `{ importLocation?: string, importAlias?: string, datasource: string }` objects to insert into the provider data source list." },
    { name: "additionalEphemeralResources", required: "false", default: "[]", description: "A list of `{ importLocation?: string, importAlias?: string, datasource: string }` objects to insert into the provider ephemeral resource list." }
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" }
  ]}
/>

## Method and parameter management
```yml
terraform:
  allowUnknownFieldsInWeakUnions: false
```

<Table 
  data={[
    { name: "allowUnknownFieldsInWeakUnions", required: "false", default: "false", description: "Allow unknown fields in weak (undiscriminated) unions." }
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" }
  ]}
/>

## Environment variables

```yml
terraform:
  environmentVariables: []
```

<Table 
  data={[
    { name: "environmentVariables", required: "false", default: "[]", description: "A list of objects with `[env: string, providerAttribute: string]` keys/values to associate environment variables with a provider variable." }
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" }
  ]}
/>


