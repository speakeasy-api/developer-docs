import { Table } from "@/mdx/components";

# Terraform configuration options

This section details the available configuration options for Terraform Provider generation. All configuration is managed in the `gen.yaml` file under the `terraform` section.

## Version and general configuration

```yml
terraform:
  version: 1.2.3
  author: "examplecorp"
  packageName: "examplecloud"
```

<Table
  data={[
    {
      name: "version",
      required: "true",
      default: "0.0.1",
      description: "Current version of the Terraform Provider.",
    },
    {
      name: "packageName",
      required: "true",
      default: "terraform",
      description: "Terraform Provider name. Prefixes all resource names. For providers published in the public Terraform Registry, this typically matches the suffix after terraform-provider- in the GitHub Repository name.",
    },
    {
      name: "author",
      required: "true",
      default: "speakeasy",
      description: "Terraform Provider namespace. For providers published in the public Terraform Registry, this typically matches the GitHub Organization name.",
    },
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" },
  ]}
/>

## Additions

```yml
terraform:
  additionalDependencies: {}
  additionalResources: []
  additionalDataSources: []
  additionalEphemeralResources: []
```

<Table
  data={[
    {
      name: "additionalDependencies",
      required: "false",
      default: "{}",
      description:
        "Add additional dependencies to include in the generated `go.mod`.",
    },
    {
      name: "additionalResources",
      required: "false",
      default: "[]",
      description:
        "A list of `{ importLocation?: string, importAlias?: string, resource: string }` objects to insert into the provider resource list.",
    },
    {
      name: "additionalDataSources",
      required: "false",
      default: "[]",
      description:
        "A list of `{ importLocation?: string, importAlias?: string, datasource: string }` objects to insert into the provider data source list.",
    },
    {
      name: "additionalEphemeralResources",
      required: "false",
      default: "[]",
      description:
        "A list of `{ importLocation?: string, importAlias?: string, datasource: string }` objects to insert into the provider ephemeral resource list.",
    },
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" },
  ]}
/>

## Environment variables

```yml
terraform:
  environmentVariables:
    - env: EXAMPLECLOUD_TOKEN
      providerAttribute: token
```

<Table
  data={[
    {
      name: "environmentVariables",
      required: "false",
      default: "[]",
      description:
        "A list of objects with `[env: string, providerAttribute: string]` keys/values to associate environment variables with a provider attribute.",
    },
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" },
  ]}
/>
