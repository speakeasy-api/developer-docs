import { Table, Callout } from "@/mdx/components";

# Python configuration options

This section details the available configuration options for the Python SDK. All configuration is managed in the `gen.yaml` file under the `python` section.

## Identity and metadata

```yml
python:
  version: 1.2.3
  templateVersion: v2
  packageName: "openapi"
  moduleName: "openapi"
  authors: ["Author Name"]
  description: "Python Client SDK Generated by Speakeasy."
  homepage: "https://example.com"
  documentationUrl: "https://example.com/docs"
  license: "Apache-2.0"
```

<Table
  data={[
    {
      name: "version",
      required: "true",
      default: "0.0.1",
      description: "The current version of the SDK.",
    },
    {
      name: "templateVersion",
      required: "false",
      default: "v2",
      description: "The template version to use. Format: `v1`, `v2`, etc.",
    },
    {
      name: "packageName",
      required: "true",
      default: "openapi",
      description:
        "The distribution name of the PyPI package. See [Python Package Metadata](https://packaging.python.org/en/latest/specifications/name-normalization/).",
    },
    {
      name: "moduleName",
      required: "false",
      default: "Same as `packageName`",
      description:
        "The name of the module to import from. Allows using a different name for imports than the package name. For example, `pip install {packageName}` and in code `from {moduleName} import SDK`. PEP 420 implicit namespace packages are supported with period (`.`) characters — e.g. `example_cloud.api_client` generates the SDK in `example_cloud/api_client/` with no `__init__.py` in the parent. [Custom code](/docs/customize/code/code-regions/overview) regions will be removed by updating `moduleName`.",
    },
    {
      name: "authors",
      required: "true",
      default: '["Speakeasy"]',
      description: "Authors of the published package.",
    },
    {
      name: "description",
      required: "false",
      default: '"Python Client SDK Generated by Speakeasy."',
      description: "A short description of the project.",
    },
    {
      name: "homepage",
      required: "false",
      default: "null",
      description: "The URL for the homepage of the project.",
    },
    {
      name: "documentationUrl",
      required: "false",
      default: "null",
      description: "The URL for the project documentation.",
    },
    {
      name: "license",
      required: "false",
      default: "`Apache-2.0` (new) / empty (legacy)",
      description:
        "The SPDX license identifier or license text for the project.",
    },
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" },
  ]}
/>

### Different package and module names

Configure a different name for the PyPI package and the module to import from:

```yml
python:
  packageName: "my-package" # Install with: pip install my-package
  moduleName: "my_module" # Import with: from my_module import SDK
```

This can be useful when the package name follows PyPI conventions (using hyphens) but the module name follows Python conventions (using underscores).

## Build and tooling

```yml
python:
  packageManager: "uv"
  additionalDependencies:
    main:
      requests: "^2.25.1"
    dev:
      pytest: "^6.2.1"
  compileCommands:
    - ["uv", "lock"]
    - ["uv", "sync", "--dev"]
    - ["uv", "run", "python", "-m", "compileall", "-q", "."]
  pytestFilterWarnings:
    - error
    - "ignore::DeprecationWarning"
  pytestTimeout: 300
```

<Table
  data={[
    {
      name: "packageManager",
      required: "false",
      default: "`uv` (new) / `poetry` (legacy)",
      description:
        "The package manager to use for dependency management and packaging. Options: `uv` (recommended) or `poetry`.",
    },
    {
      name: "additionalDependencies",
      required: "false",
      default: "`{main: {}, dev: {}}`",
      description:
        "Specify additional dependencies to include in the generated `pyproject.toml` file.",
    },
    {
      name: "compileCommands",
      required: "false",
      default: "N/A",
      description:
        "A list of commands to use for compiling the SDK, replacing the default compile pipeline. Must be an array of arrays where each inner array has the command as the first element and the rest are arguments.",
    },
    {
      name: "pytestFilterWarnings",
      required: "false",
      default: "`[]`",
      description:
        "When the array contains any strings, sets the global pytest `filterwarnings` value (filters to control Python warnings, e.g. ignoring or raising as errors). See [Python warning filters](https://docs.python.org/3/library/warnings.html#warning-filter).",
    },
    {
      name: "pytestTimeout",
      required: "false",
      default: "0",
      description:
        "When greater than 0, installs `pytest-timeout` and sets the global timeout (in seconds) before individual tests are timed out.",
    },
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" },
  ]}
/>

<Callout title="Default compilation pipeline" type="info">
When `compileCommands` is not set, the default pipeline runs based on the package manager (`uv` or `poetry`) and includes bytecode compilation (`compileall`), linting (`pylint -j=0`), and type checking (`mypy` and `pyright` in parallel). To reduce pylint parallelism and avoid OOM errors in CI, use `compileCommands` with `-j=1`.
</Callout>

## API surface shape

```yml
python:
  maxMethodParams: 999
  flattenRequests: true
  flatteningOrder: "parameters-first"
  methodArguments: "infer-optional-args"
  flattenGlobalSecurity: true
  envVarPrefix: "SPEAKEASY"
  responseFormat: "flat"
  multipartArrayFormat: "standard"
  imports:
    option: "openapi"
    paths:
      callbacks: "models/callbacks"
      errors: "models/errors"
      operations: "models/operations"
      shared: "models/shared"
      webhooks: "models/webhooks"
```

<Table
  data={[
    {
      name: "[maxMethodParams](/docs/customize/methods)",
      required: "false",
      default: "`999` (new) / `4` (legacy)",
      description:
        'The maximum number of parameters a method can have before the resulting SDK endpoint is no longer "flattened" and an input object is generated instead. `0` means input objects are always used.',
    },
    {
      name: "flattenRequests",
      required: "false",
      default: "true (new SDKs)",
      description:
        "Turn request parameters and body fields into a flat list of method arguments. Takes precedence over `maxMethodParams`. If there is no request body, `maxMethodParams` is respected.",
    },
    {
      name: "flatteningOrder",
      required: "false",
      default: "parameters-first",
      description:
        "When flattening parameters and body fields, determines the ordering of generated method arguments. Options: `parameters-first` or `body-first`.",
    },
    {
      name: "methodArguments",
      required: "false",
      default: "infer-optional-args",
      description:
        "If `infer-optional-args`, when all parameters and the request body are optional, the argument to the function will be optional. Alternative: `require-security-and-request`.",
    },
    {
      name: "[flattenGlobalSecurity](/docs/customize/authentication/overview)",
      required: "false",
      default: "true",
      description:
        "Flatten the global security configuration if there is only a single option in the spec.",
    },
    {
      name: "envVarPrefix",
      required: "false",
      default: '""',
      description:
        "The environment variable prefix for security and global env variable overrides. If empty, these overrides will not be possible.",
    },
    {
      name: "[responseFormat](/docs/customize/responses/responses)",
      required: "false",
      default: "flat",
      description:
        "Determines the shape of the response envelope returned from SDK methods. Options: `envelope`, `envelope-http`, or `flat`.",
    },
    {
      name: "multipartArrayFormat",
      required: "false",
      default: "`standard` (new) / `legacy`",
      description:
        "Format for array field names in multipart form data. `legacy` appends `[]` (e.g. `files[]`). `standard` uses the field name as-is.",
    },
    {
      name: "imports",
      required: "false",
      default: "{}",
      description:
        "Configuration for model import structure (paths for `shared`/`operations`/`errors`/`callbacks`/`webhooks`).",
    },
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" },
  ]}
/>

### Import paths

<Table
  data={[
    {
      component: "callbacks",
      default: "models/callbacks",
      description: "The directory where callback models are imported from.",
    },
    {
      component: "errors",
      default: "models/errors",
      description: "The directory where error models are imported from.",
    },
    {
      component: "operations",
      default: "models/operations",
      description:
        "The directory where operation models (i.e., API endpoints) are imported from.",
    },
    {
      component: "shared",
      default: "models/components",
      description:
        "The directory for shared components, such as reusable schemas and data models imported from the OpenAPI spec.",
    },
    {
      component: "webhooks",
      default: "models/webhooks",
      description:
        "The directory for webhook models, if the SDK includes support for webhooks.",
    },
  ]}
  columns={[
    { key: "component", header: "Component" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" },
  ]}
/>

## Async and SSE

```yml
python:
  asyncMode: split
  useAsyncHooks: false
  sseFlatResponse: false
```

<Table
  data={[
    {
      name: "asyncMode",
      required: "false",
      default: "both",
      description:
        "Whether to generate sync and/or async methods. `both` generates one constructor with `_async`-suffixed methods. `split` generates `FooSDK` and `AsyncFooSDK` with no method-name suffixes.",
    },
    {
      name: "useAsyncHooks",
      required: "false",
      default: "false",
      description:
        "Enable async hooks infrastructure for async SDK methods. SDK maintainers must explicitly register async hooks in `asyncregistration.py`. Adapters wrap sync hooks, but native async implementations are recommended.",
    },
    {
      name: "sseFlatResponse",
      required: "false",
      default: "false",
      description:
        "Whether to flatten SSE (Server-Sent Events) responses by extracting the `data` field from wrapper models, providing direct access to event data instead of the wrapper object.",
    },
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" },
  ]}
/>

The `asyncMode` setting provides two patterns for handling async operations:

**Method-based (`both`, default)**: Every operation has two methods — a synchronous version and an asynchronous version with an `_async` suffix.

```python
sdk = MyAPI(api_key="...")

# Synchronous operations
result = sdk.list_users()

# Asynchronous operations
result = await sdk.list_users_async()
```

**Constructor-based (`split`)**: Separate constructors for synchronous and asynchronous clients. All method names are identical between sync and async versions.

```python
# Synchronous client
sync_sdk = MyAPI(api_key="...")
result = sync_sdk.list_users()

# Asynchronous client
async_sdk = AsyncMyAPI(api_key="...")
result = await async_sdk.list_users()
```

The constructor-based pattern eliminates method name duplication and provides clearer IDE suggestions.

<Callout title="Breaking change" type="warning">
Switching to `asyncMode: split` is a breaking change. Existing SDK consumers will need to update their code to use the new constructor pattern.
</Callout>

## Errors

```yml
python:
  clientServerStatusCodesAsErrors: true
  defaultErrorName: ""
  baseErrorName: "APIError"
```

<Table
  data={[
    {
      name: "clientServerStatusCodesAsErrors",
      required: "false",
      default: "true",
      description: "Whether to treat 4xx and 5xx status codes as errors.",
    },
    {
      name: "defaultErrorName",
      required: "false",
      default: "empty (new) / `SDKError` (legacy)",
      description:
        "The name of the default exception raised when an API error occurs.",
    },
    {
      name: "baseErrorName",
      required: "false",
      default: "null",
      description:
        "The name of the base error class used for HTTP error responses.",
    },
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" },
  ]}
/>

## Enums and unions

```yml
python:
  enumFormat: "union"
  forwardCompatibleEnumsByDefault: true
  forwardCompatibleUnionsByDefault: "tagged-only"
  inferUnionDiscriminators: true
  preApplyUnionDiscriminators: true
```

<Table
  data={[
    {
      name: "enumFormat",
      required: "false",
      default: "`union` (new) / `enum` (legacy)",
      description:
        "Determines the format to express enums in Python. Options: `union` or `enum`.",
    },
    {
      name: "forwardCompatibleEnumsByDefault",
      required: "false",
      default: "true (new SDKs)",
      description:
        "When true, any enum used in a response is automatically open/forward-compatible — unknown values are tolerated. Single-value enums are not auto-opened. Individual enums can be controlled with `x-speakeasy-unknown-values: allow/disallow`.",
    },
    {
      name: "forwardCompatibleUnionsByDefault",
      required: "false",
      default: "`tagged-only` (new) / `false` (legacy)",
      description:
        "Controls forward compatibility for discriminated unions in responses. `tagged-only` makes discriminated unions open to unknown discriminator values. `false` disables. Individual unions can be controlled with `x-speakeasy-unknown-values: allow/disallow`.",
    },
    {
      name: "inferUnionDiscriminators",
      required: "false",
      default: "true",
      description:
        "Infer union discriminators for `oneOf`s missing explicit OpenAPI discriminator mapping.",
    },
    {
      name: "preApplyUnionDiscriminators",
      required: "false",
      default: "true (new SDKs)",
      description:
        "When true, discriminator values are applied as const fields onto union member types when those types are consistently used with the same discriminator value. Simplifies type structure and enables `Annotated[Union[...], Field(discriminator='...')]` syntax.",
    },
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" },
  ]}
/>

## Models and miscellaneous

```yml
python:
  inputModelSuffix: "input"
  outputModelSuffix: "output"
  constFieldCasing: "normal"
  allowedRedefinedBuiltins:
    - id
    - object
    - input
    - dir
  enableCustomCodeRegions: false
```

<Table
  data={[
    {
      name: "inputModelSuffix",
      required: "false",
      default: "input",
      description:
        "Suffix added to models with writeOnly fields that are generated as input models.",
    },
    {
      name: "outputModelSuffix",
      required: "false",
      default: "output",
      description:
        "Suffix added to models with readOnly fields that are generated as output models.",
    },
    {
      name: "constFieldCasing",
      required: "false",
      default: "`normal` (new) / `upper` (legacy)",
      description:
        "Casing convention for const fields in generated models. `upper` uses `SCREAMING_SNAKE_CASE`, `normal` uses `snake_case`.",
    },
    {
      name: "allowedRedefinedBuiltins",
      required: "false",
      default:
        '`["id", "object", "input", "dir"]` (new) / `["id", "object", "dir"]` (legacy)',
      description:
        "List of names allowed to shadow builtins via SDK method parameters.",
    },
    {
      name: "[enableCustomCodeRegions](/docs/customize/code/code-regions/overview)",
      required: "false",
      default: "false",
      description:
        "Allow custom code to be inserted into the generated SDK.",
    },
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" },
  ]}
/>

## Fixes

Fixes to apply to the generated SDK. Generally `true` for new SDKs but may be `false` for backward compatibility with existing SDKs.

```yml
python:
  fixes:
    responseRequiredSep2024: true
    asyncPaginationSep2025: true
    conflictResistantModelImportsFeb2026: true
```

<Table
  data={[
    {
      name: "responseRequiredSep2024",
      required: "false",
      default: "true (new SDKs)",
      description:
        "Enables fixes introduced in September 2024 for response required field handling. For existing SDKs, setting to true is recommended but may be a breaking change.",
    },
    {
      name: "asyncPaginationSep2025",
      required: "false",
      default: "true (new SDKs)",
      description:
        "Disabling not recommended. Enables changes introduced in September 2025 to fix async pagination methods to return async `next()` functions instead of synchronous ones, preventing blocking fetches after the initial API call. For existing SDKs, setting to true is recommended but will be a breaking change.",
    },
    {
      name: "conflictResistantModelImportsFeb2026",
      required: "false",
      default: "true (new SDKs)",
      description:
        "Enables conflict-resistant model imports introduced in February 2026. For existing SDKs, setting to true is recommended but may be a breaking change.",
    },
  ]}
  columns={[
    { key: "name", header: "Name" },
    { key: "required", header: "Required" },
    { key: "default", header: "Default Value" },
    { key: "description", header: "Description" },
  ]}
/>
