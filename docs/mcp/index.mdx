---
tableOfContents: false
title: Docs Home
description: "Read Gram documentation for more information on our features and how to use them."
alwaysShowInSidebar: true
hideTitle: true
---

import SiteAsciiEnvironment from "@/components/astro/site-ascii-environment";
import GramDocsHome from "@/components/astro/docs/GramDocsHome";

{/*
  * Renders through the Starlight docs chrome (sidebar + footer) but the hub
  * itself is a hydrated island so the header WebGL hero animates, the cards use
  * their component styling (not prose), and it sits at full width instead of
  * the cramped prose column. SiteAsciiEnvironment is the shared WebGL canvas
  * the hero injects into. `hideTitle` drops Starlight's redundant page-title
  * H1 (the hero already shows the title); `tableOfContents: false` drops the
  * right-hand TOC.
  */}

<SiteAsciiEnvironment client:only="react" />
<GramDocsHome client:load />
