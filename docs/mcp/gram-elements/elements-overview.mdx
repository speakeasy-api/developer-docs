---
title: Introduction to Gram Elements
description: An intro to Gram Elements, a UX toolkit for building AI chat experiences.
---

import { CardGrid, CodeWithTabs, CalloutCta } from "@/mdx/components";
import { mainFeaturesCards } from "@/lib/data/docs/elements/features-data";
import { Icon } from "@/components/ui/icon";
import { Button } from "@/components/ui/button";
import { ArrowUpRight } from "lucide-react";

<CalloutCta
  icon={<Icon name="gram" className="size-6 shrink-0 stroke-primary" />}
  title="Become a design partner"
  description="We're looking for early adopters to help shape the future of Gram Elements. Get early access to new features, influence the roadmap, and receive dedicated support from our team."
  buttonText="Talk to our team"
  buttonHref="/book-demo"
/>

<div className="flex flex-col sm:flex-row sm:items-center gap-4 mb-4 max-w-[68.75rem] mx-auto">
  <h2 className="text-3xl md:text-4xl font-bold mb-1! sm:mb-6! my-6!">Gram Elements</h2>
</div>

A batteries-included UX toolkit that makes building AI chat experiences easy and intuitive. Ship production-ready chat interfaces in minutes, not weeks, with full theming support, responsive layouts, and built-in MCP integration.

The `<Chat />` component comes with the following features:

<CardGrid cards={mainFeaturesCards} columns={3} />

## Start building with Gram Elements

Install the Gram Elements package and see [our quickstart guide](/docs/mcp/gram-elements/quickstart) to get your first chat-based experience running in minutes.

<CodeWithTabs
  tabs={[
    { label: "npm", language: "bash", code: "npm install @gram-ai/elements" },
    { label: "pnpm", language: "bash", code: "pnpm add @gram-ai/elements" },
    { label: "yarn", language: "bash", code: "yarn add @gram-ai/elements" },
  ]}
/>

<div className="flex flex-col sm:flex-row sm:items-center justify-center gap-4 mt-8 max-w-275 mx-auto">
  <h2 className="text-3xl md:text-4xl font-bold  mb-1! sm:mb-6! my-6!">Ready to add AI chat to your product?</h2>
  <Button variant="rainbow" href="https://app.getgram.ai" className="shrink-0 [&_p]:font-mono!">
    Build your own
    <ArrowUpRight className="size-4" />
  </Button>
</div>
