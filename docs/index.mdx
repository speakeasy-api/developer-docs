---
title: Documentation
description: "Read Speakeasy documentation for more information on our features and how to use them."
alwaysShowInSidebar: true
tableOfContents: false
hideTitle: true
---

import SiteAsciiEnvironment from "@/components/astro/site-ascii-environment";
import DocsHome from "@/components/astro/docs/DocsHome";

{/*
  * The /docs landing renders inside the Starlight docs chrome (PageFrame
  * override) so it keeps the docs sidebar and the footer that clears the
  * content — the things a standalone page dropped. The hub itself is a single
  * hydrated island (DocsHome, client:load) so the header WebGL hero animates
  * and the CLI install tabs switch; SiteAsciiEnvironment provides the shared
  * WebGL canvas the hero injects into (client:only — it's browser-only R3F).
  * `tableOfContents: false` removes the right-hand TOC (this is a card hub,
  * not prose).
  */}

<SiteAsciiEnvironment client:only="react" />
<DocsHome client:load />
